/*
 * server
 *
 * ## Authentication  Authenticate with the API using [Basic Auth](https://en.wikipedia.org/wiki/Basic_access_authentication), get API keys in the project settings:  - username: Langfuse Public Key - password: Langfuse Secret Key  ## Exports  - OpenAPI spec: https://cloud.langfuse.com/generated/api/openapi.yml - Self-hosted deployments: interactive API reference at `/api/docs` and OpenAPI spec at `/api/openapi.yaml`
 *
 * The version of the OpenAPI document:
 *
 * Generated by: https://openapi-generator.tech
 */

use crate::models;
use serde::{Deserialize, Serialize};

/// PromptVariableMappingSource : Source field used to populate a prompt variable.  Use these values when mapping evaluator prompt variables to live data.  Source semantics: - `input`: the observation input payload - `output`: the observation output payload - `metadata`: the observation metadata object. Combine with `jsonPath` when you need one nested field instead of the whole object. - `tool_calls`: the tool calls recorded on the observation, as an array of `{id, name, arguments, type, index}` objects in the order the model emitted them. Combine with `jsonPath` (for example `$[*].name`) to select parts of each call. - `expected_output`: the experiment item's expected output when the observation belongs to an experiment. - `experiment_item_metadata`: the experiment item's metadata when the observation belongs to an experiment.
/// Source field used to populate a prompt variable.  Use these values when mapping evaluator prompt variables to live data.  Source semantics: - `input`: the observation input payload - `output`: the observation output payload - `metadata`: the observation metadata object. Combine with `jsonPath` when you need one nested field instead of the whole object. - `tool_calls`: the tool calls recorded on the observation, as an array of `{id, name, arguments, type, index}` objects in the order the model emitted them. Combine with `jsonPath` (for example `$[*].name`) to select parts of each call. - `expected_output`: the experiment item's expected output when the observation belongs to an experiment. - `experiment_item_metadata`: the experiment item's metadata when the observation belongs to an experiment.
#[derive(Clone, Copy, Debug, Eq, PartialEq, Ord, PartialOrd, Hash, Serialize, Deserialize)]
pub enum PromptVariableMappingSource {
    #[serde(rename = "input")]
    Input,
    #[serde(rename = "output")]
    Output,
    #[serde(rename = "metadata")]
    Metadata,
    #[serde(rename = "tool_calls")]
    ToolCalls,
    #[serde(rename = "expected_output")]
    ExpectedOutput,
    #[serde(rename = "experiment_item_metadata")]
    ExperimentItemMetadata,
}

impl std::fmt::Display for PromptVariableMappingSource {
    fn fmt(&self, f: &mut std::fmt::Formatter) -> std::fmt::Result {
        match self {
            Self::Input => write!(f, "input"),
            Self::Output => write!(f, "output"),
            Self::Metadata => write!(f, "metadata"),
            Self::ToolCalls => write!(f, "tool_calls"),
            Self::ExpectedOutput => write!(f, "expected_output"),
            Self::ExperimentItemMetadata => write!(f, "experiment_item_metadata"),
        }
    }
}

impl Default for PromptVariableMappingSource {
    fn default() -> PromptVariableMappingSource {
        Self::Input
    }
}
