/*
 * server
 *
 * ## Authentication  Authenticate with the API using [Basic Auth](https://en.wikipedia.org/wiki/Basic_access_authentication), get API keys in the project settings:  - username: Langfuse Public Key - password: Langfuse Secret Key  ## Exports  - OpenAPI spec: https://cloud.langfuse.com/generated/api/openapi.yml
 *
 * The version of the OpenAPI document:
 *
 * Generated by: https://openapi-generator.tech
 */

use crate::models;
use serde::{Deserialize, Serialize};

/// UnstableEvaluationRuleReadMapping : Maps an evaluator variable to target data, as returned by read endpoints.  Reads are more permissive than writes in two ways, so that data configured before a migration stays readable and repairable: - `source` is `null` when a mapping was never fully configured. Set a concrete `source` to fix it. - every `source` value is allowed regardless of the rule `target`, because an evaluator's   default mapping is stored independently of the rules that use it.  Requests always require a concrete `source` that is valid for the selected `target`.
#[derive(Clone, Default, Debug, PartialEq, Serialize, Deserialize, bon::Builder)]
pub struct UnstableEvaluationRuleReadMapping {
    /// Prompt variable name without braces.
    #[serde(rename = "variable")]
    pub variable: String,
    #[serde(rename = "source")]
    pub source: models::UnstableEvaluationRuleMappingSource,
    /// Optional JSONPath selector applied to the selected source.
    #[serde(
        rename = "jsonPath",
        default,
        with = "::serde_with::rust::double_option",
        skip_serializing_if = "Option::is_none"
    )]
    pub json_path: Option<Option<String>>,
}

impl UnstableEvaluationRuleReadMapping {
    /// Maps an evaluator variable to target data, as returned by read endpoints.  Reads are more permissive than writes in two ways, so that data configured before a migration stays readable and repairable: - `source` is `null` when a mapping was never fully configured. Set a concrete `source` to fix it. - every `source` value is allowed regardless of the rule `target`, because an evaluator's   default mapping is stored independently of the rules that use it.  Requests always require a concrete `source` that is valid for the selected `target`.
    pub fn new(
        variable: String,
        source: models::UnstableEvaluationRuleMappingSource,
    ) -> UnstableEvaluationRuleReadMapping {
        UnstableEvaluationRuleReadMapping {
            variable,
            source,
            json_path: None,
        }
    }
}
