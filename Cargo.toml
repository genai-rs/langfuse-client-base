[package]
name = "langfuse-client-base"
version = "0.5.1"
edition = "2021"
authors = ["Tim Van Wassenhove <github@timvw.be>"]
license = "MIT OR Apache-2.0"
description = "Auto-generated Langfuse API client from OpenAPI specification"
repository = "https://github.com/genai-rs/langfuse-client-base"
homepage = "https://github.com/genai-rs/langfuse-client-base"
documentation = "https://docs.rs/langfuse-client-base"
readme = "README.md"
keywords = ["langfuse", "openapi", "client", "api", "llm"]
categories = ["api-bindings", "web-programming::http-client"]
rust-version = "1.83"

[package.metadata.docs.rs]
# Build with default features (rustls). Keep builds deterministic on docs.rs
features = ["rustls"]
no-default-features = false
rustdoc-args = ["--cfg", "docsrs"]

[dependencies]
bon = "3.8.1"
serde = { version = "^1.0.228", features = ["derive"] }
serde_with = { version = "^3.15.1", default-features = false, features = ["base64", "std", "macros"] }
serde_json = "^1.0.145"
serde_repr = "^0.1.20"
url = "^2.5.7"
reqwest = { version = "^0.12.24", default-features = false, features = ["json", "multipart"] }
reqwest-middleware = { version = "^0.4.2", features = ["json", "multipart"] }

[dev-dependencies]
tokio = { version = "1.48.0", features = ["full"] }

[features]
default = ["rustls"]
rustls = ["reqwest/rustls-tls"]
native-tls = ["reqwest/native-tls"]

[lints.clippy]
# Allow all clippy warnings for generated code
all = "allow"
pedantic = "allow"
restriction = "allow"
nursery = "allow"
cargo = "allow"
